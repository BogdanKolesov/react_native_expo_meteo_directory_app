import React from 'react';
import { StyledPicture } from './Picture.styles'

const Picture = ({ source }) => {
    return (
        <StyledPicture source={source} />
    );
}


export default Picture;
